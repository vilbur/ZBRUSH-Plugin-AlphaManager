/** enableMaskButton
  */
[RoutineDef, enableMaskButton
,
	//[Note, "enableMaskButton"]
	[VarSet, MaskButtonPath, [StrMerge, $MAKS_BUTTONS, "Mask ", $alpha_id ]]
	
	
	//[If, [IExists, MaskButtonPath ] 
	//, // THEN
		[IEnable, MaskButtonPath ]
	//
	//, // ELSE
	//	[NoteBar, [StrMerge, "------------------------------- WARNING: Mask Button Not Found: ", MaskButtonPath ]]
	//]

, // PARAMS
	$alpha_id
]
//[IButton, "enableMaskButton()" , "", [RoutineCall, enableMaskButton, 1] ]